''''''
'''
图像分类(类别)
将整副图像归类到某个预定义的类别中
目标是识别图像的主要内容,并将图像分配到一个类别标签,
图像分类只回答这幅图像属于哪个类别

目标检测(类别+位置)
不仅要识别图像中的物体类别,还要确定这些物体在图像中的位置
目标检测的输出通常保罗一个或多个边界框,每个框对弈一个检测到的物体,以及这些物体的类别标签

目标检测两种方式进行:
单阶段和两阶段
两阶段检测更准(代表算法:R-CNN Fast R-CNN,Faster R-CNN,Mask R-CNN)
单阶段检测更快(YOLO系列,SSD,RetinaNet,EffcientDet)


双阶段:(先生成候选区域,在分类定位)
第一阶段通过一些算法或者模块生成候选区域,实际上在做的寻找前景(感兴趣的,即要检测的目标,不感兴趣的叫背景)
第二阶段对这些候选区域进行分类和精确定位
单阶段l
R-CNN
1. R-CNN（目标检测深度学习开山之作）
核心原理：3 步走
① 用「选择性搜索」生成候选区域；② 裁剪候选区域并通过 CNN 提取特征；③ 用 SVM 分类 + 边界框回归定位。
必背优点：首次将深度学习引入目标检测，大幅提升精度。
必背缺点：速度极慢（每个候选区域单独提特征，计算冗余）


2. Fast R-CNN（优化速度瓶颈）
核心改进：整幅图像仅做 1 次 CNN 特征提取，候选区域通过「ROI 池化」统一尺寸。
必背优点：速度比 R-CNN 快，解决单区域重复提特征问题。
必背缺点：候选区域仍依赖选择性搜索，速度瓶颈未完全解决。

3. Faster R-CNN（两阶段成熟标杆）
核心改进：用「区域建议网络（RPN）」替代选择性搜索，从特征图直接生成候选区域（锚框机制）。
必背优点：将候选区域生成与检测整合到同一网络，速度和精度双提升。
必背缺点：相比单阶段模型，结构复杂、计算量仍较大。

4. Mask R-CNN（检测 + 分割拓展）
核心改进：在 Faster R-CNN 基础上增加「卷积分支」，输出像素级分割掩码（mask）。
必背优点：同时支持目标检测 + 实例分割（精确到像素）。
必背缺点：结构更复杂，训练 / 计算成本高。






------------------------------------







单阶段
速度快,结构简单
YOLO (You Only Look Once) 

YOLOv1
整幅图像一次性输入网络,网络输出特征图
特征图被划分为S * S 的网格,每个网格负责检测多个对象
每个网络预测固定数量的边界框及其置信度分数和类别概率

优点:
速度快,适用于实时监测任务
直接在整个图形上进行检测,避免了候选区域生成过程

缺点
检测小物体的效果较差,因为每个网格只能负责有限数量的边界框
预测结果不够精确,相比两阶段模型精度较低


YOLOv2YOLOv3
引入了锚框机制,提高了检测灵感性和精度
增加了多尺度特征图用于检测不同大小的物体
YOLOv3进一步增加了检测层数,提高了小物体的检测性能

相比于YOLOv1,精度和稳定性显著提升
保持了高检测速度,适用于实时应用



SSD:
使用卷积神经网络提取特征图
在不同尺度的特征图上进行预测,生产边界框及其类别概率
采用多尺度特征图用来捕捉不同大小的物体

在各个特征层上进行预测,可以处理不同尺寸的物体
采用默认边界框机制,每个位置预测多个不同尺寸和长宽比的边界框


RetinaNet（单阶段精度突破）
核心改进：引入「焦点损失（Focal Loss）」，解决类别不平衡问题（难检测物体关注不足）。
必背优点：单阶段模型中首次实现与两阶段媲美的精度。
必背缺点：结构比 YOLO/SSD 复杂，计算量较大。

EfficientDet（高效能标杆）
核心原理：基于 EfficientNet 骨干网络 +「双向特征金字塔（BiFPN）」，结合 NAS 优化结构。
必背优点：精度和效率双优，可根据计算资源灵活调整。
必背缺点：复杂度高，训练 / 部署需更多资源





目标检测与图像分类的本质区别：是否需要 “定位物体位置”（输出边界框）。
两阶段 vs 单阶段的核心权衡：两阶段准，单阶段快。
Faster R-CNN 的核心创新：用 RPN 替代选择性搜索，解决候选区域生成的速度瓶颈。
YOLO 的核心优势：端到端实时检测，适用于对速度要求高的场景。
RetinaNet 的关键突破：焦点损失解决类别不平衡，让单阶段模型精度追平两阶段。
边界框表示：统一用「[x1,y1,x2,y2]」（左上角 x/y 坐标 + 右下角 x/y 坐标）。
类别标签：所有算法的输出均为「类别索引（0/1/2…）」，而非直接输出文字。














'''